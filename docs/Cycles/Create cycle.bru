meta {
  name: Create cycle
  type: http
  seq: 4
}

post {
  url: http://{{URL}}/organizations?name=Dunder
  body: none
  auth: bearer
}

params:query {
  name: Dunder
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
